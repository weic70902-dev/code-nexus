# 配置说明

## 应用配置

Code Nexus 应用可以通过以下选项进行配置：

```javascript
const app = createApp({
  // 应用根元素
  root: '#app',
  
  // 是否启用开发模式
  debug: false,
  
  // 插件列表
  plugins: []
})
```

## 更多配置项

请参考 [API 参考](/reference/api) 获取完整配置选项列表。